import week from './week';
import chemicalElement from './chemicalElement';
import fruit from './fruit';

const result = { chemicalElement, fruit, week };

window.mumu = result;
export default result;
